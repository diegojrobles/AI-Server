# Test and lint environment. Deliberately excludes torch/transformers:
# the model is faked in tests, so CI installs in seconds instead of minutes.
-r requirements-base.txt

pytest==9.0.3
pytest-cov==7.0.0
ruff==0.6.9
