version: '3.8'

services:
  ai-server:
    build: .
    ports:
      - "5000:5000"
    environment:
      - HOST=0.0.0.0
      - PORT=5000
      - DEBUG=False
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped