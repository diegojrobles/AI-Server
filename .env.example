# Copy to .env and fill in. Never commit .env.

HOST=0.0.0.0
PORT=5000
DEBUG=False

MODEL_NAME=distilbert-base-uncased-finetuned-sst-2-english
MAX_LENGTH=512

# Clients must send this as the X-API-Key header. Leave unset to disable auth.
API_KEY=change-me

RATELIMIT_ENABLED=True
